{
  "name": "barrage",
  "version": "1.0.0",
  "private": true,
  "description": "Barrage — a single-file artillery game. index.html is the whole app; this file exists only to run the tests.",
  "scripts": {
    "test": "node --test "tests/*.test.mjs""
  }
}
